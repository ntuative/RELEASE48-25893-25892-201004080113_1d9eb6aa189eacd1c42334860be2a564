package com.sulake.habbo.avatar.structure
{
   public interface IStructureData
   {
       
      
      function append(param1:XML) : Boolean;
      
      function parse(param1:XML) : Boolean;
   }
}
